- check that only allowed things are printed to stdout

- free buffer after sending in network_udp
- check that all mallocs are being freed
- check the getline function
- validate input for every command (if it is wrong ignore or warn user, based on assignment)

use a message queue

free members on deserialization malloc fail using struct free functions
when an unknown message is received handle correctly
on receive hostname check should be for both hostanme and ip
handle malformed reply messages

do ip translation

try to lower default array size values to see if they automatically resize

put command array of strings and func pointers to different file
validation of user inputs

add packet deduplication mechanism to all message types

quitting works however authing and quitting segfaults
